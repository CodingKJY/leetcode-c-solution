#ifndef __UTILS_H__
#define __UTILS_H__

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <math.h>
#include "uthash.h"
#include "utlist.h"

#define ARRAY_SIZE(arr) sizeof(arr)/sizeof(arr[0])
#define MIN(x, y) { x <= y ? x : y }
#define MAX(x, y) { x >= y ? x : y }

#endif /* __UTILS_H__ */